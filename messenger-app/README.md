# Messenger App

Full-stack messenger application built with:
- React + Vite + Tailwind
- Node.js + Express + SQLite
- Docker

## Run without Docker

**Frontend**

cd frontend
npm install
npm run dev

**Backend**

cd server
npm install
npm run dev

## Run with Docker

docker-compose up --build
